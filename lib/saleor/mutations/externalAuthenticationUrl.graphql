mutation externalAuthenticationUrl($input: JSONString!) {
  externalAuthenticationUrl(pluginId: "mirumee.authentication.openidconnect", input: $input) {
    errors {
      code
      message
    }
    authenticationData
  }
}
