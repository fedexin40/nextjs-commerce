mutation externalObtainAccessTokens($input: JSONString!) {
  externalObtainAccessTokens(input: $input, pluginId: "mirumee.authentication.openidconnect") {
    errors {
      code
      message
    }
    refreshToken
    token
  }
}
