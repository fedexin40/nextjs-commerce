query GetShippingMethods($id: ID!) {
  checkout(id: $id) {
    shippingMethods {
      description
      id
      message
      maximumDeliveryDays
      name
      price {
        amount
        currency
      }
    }
  }
}
