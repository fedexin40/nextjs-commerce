query LastCheckout {
  me {
    checkouts(first: 10) {
      edges {
        node {
          id
          metafields(keys: "completed_with_paypal")
          transactions {
            events {
              type
              createdAt
            }
          }
        }
      }
    }
  }
}
