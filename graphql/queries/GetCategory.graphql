query GetCategory($first: Int!) {
  categories(first: $first) {
    edges {
      node {
        id
        name
        backgroundImage {
          url
          altText: alt
        }
      }
    }
  }
}
